# Copy to .env and fill values to enable PostgreSQL
DB_USER=quizuser
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_NAME=quizdb
# Optional: DB_PORT defaults to 5432
DB_PORT=5432

# App port (optional)
PORT=3000
